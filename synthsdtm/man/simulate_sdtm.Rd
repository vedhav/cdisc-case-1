% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_sdtm}
\alias{simulate_sdtm}
\title{Generate synthetic SDTM datasets}
\usage{
simulate_sdtm(
  config,
  spec,
  ct = list(),
  n_subjects = NULL,
  seed = 1234,
  domains = NULL
)
}
\arguments{
\item{config}{Parsed study config (list) or path to its JSON.}

\item{spec}{Resolved spec (list) or path to \code{sdtm_spec.json}.}

\item{ct}{Controlled Terminology cache (list) or path to \code{ct_cache.json}.}

\item{n_subjects}{Subject count; defaults to the summed cohort sizes.}

\item{seed}{Integer RNG seed.}

\item{domains}{Optional character vector restricting which domains to build.}
}
\value{
A \code{sdtm_sim} list: \code{domains} (named list of data.frames), \code{lineage},
\code{summary}, \code{datasets}, \code{variables}, plus the inputs for reproducibility.
}
\description{
The study-agnostic engine: the study lives entirely in \code{config}, the standard
in \code{spec}/\code{ct}. Deterministic and seeded — same inputs and seed yield
identical output, and shape-domain row counts are seed-independent.
}
